/*
 * @Description: 
 * @Version: 2.0
 * @Autor: zouyawen
 * @Date: 2022-08-25 16:18:51
 * @LastEditors: zouyawen
 * @LastEditTime: 2022-12-16 11:24:18
 */
// import Enums from 'utils/Enums';

// const enums = [
//   (new Enums('启用禁用')).setAlias('禁用启用').set('启用', '1').set('禁用', '0'),

// ];

// const getEnums = enumsName => {
//   return enums.find(item => item.is(enumsName));
// };

// export default getEnums;

// export const getEnumsOption = (enumsName) => {
//   const data = enums.find((item) => item.is(enumsName)).data;
//   const option = [];
//   Object.keys(data).forEach((key) => {
//     option.push({ label: key, value: data[key] });
//   });
//   return option;
// };